This is the link to the question : - https://codeforces.com/contest/112/problem/A 

My First trail:-
  
#include<iostream>
using namespace std;

int main(){
    char str1[];    // C++ must know the size of a char array---> MISTAKE
    char str2[];
    for(int i=0; i< str1.size();i++){    // size() DOES NOT exist for char[]. FOR CHAR ARRAYS USE strlen(str1) .
        cin >> str1[i];                  // size() works only for string
    }
    for(int i=0; i<str2.size();i++){
        cin >> str2[i];
    }

    for(int i=0;i<str1.size();i++){
        if((str1[i]-'0') < (str2[i]-'0')){
            cout << -1;
        }else if ((str1[i]-'0') > (str2[i]-'0')){
            cout << 1;
        }else{
            cout << 0;
        }
    }
}

This code contained several mistakes.
1.My loop prints -1/1/0 for every character.
2.My code compares 'a' and 'A' , but we have to consider them equal since we have to do lexicographically.
3.'a' - '0'   This comparision is meaningless here. we had used '0' when we had strings of number and like '0'= 48 and '1'= 49 so we did subtraction there.


Second correct answer:-

#include <iostream>
#include <string>
#include <cctype>
using namespace std;

int main() {
    string s1, s2;
    cin >> s1 >> s2;

    for (int i = 0; i < s1.length(); i++) {
        char c1 = tolower(s1[i]);
        char c2 = tolower(s2[i]);

        if (c1 < c2) {
            cout << -1;
            return 0;             // When we use return 0 inside loop, the comparision stops as soon as we get the answer.
        } else if (c1 > c2) {
            cout << 1;
            return 0;
        }
    }

    cout << 0;
    return 0;
}
